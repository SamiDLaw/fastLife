<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>{{ title }}</title>
	</head>
	<body>
		<h1>Événements à venir</h1>
		<ul>
			{% for event in events %}
				<li>{{ event.title }}
					-
					{{ event.date }}</li>
			{% endfor %}
		</ul>
	</body>
</html>
